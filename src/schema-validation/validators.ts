import { ValidationTypes } from "./internal/helper-types";



function validateType(value : any, validateDefinition : ValidationTypes)
{
    
    switch(validateDefinition.kind)
    {
        case 'array': {
            const v = validateDefinition;
           
            
            // if ()

            return;

        }

        case 'array' : {
            
        }

    }
}